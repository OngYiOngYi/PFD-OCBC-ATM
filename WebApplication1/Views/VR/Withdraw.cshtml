@{
    ViewData["Title"] = "Withdraw";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Withdraw</title>
    <style>
        body {
            font-family: 'Comfortaa', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            overflow:hidden;
        }

        .seperation {
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 100vh;
            width: 100%; /* Cover the full width of the website */
        }

        .mains {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            background-color:white;
            height: 100%; /* Updated to 100% to cover the full height of the container */
            width: 75%;
        }

        .right-side {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100%; /* Updated to 100% to cover the full height of the container */
            width: 40%;
        }

        header {
            text-align: center;
        }

        .withdrawtext {
            text-align: center;
            margin: 20px 0;
            font-size: 18px;
            color: #555;
        }

        .input-group form {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            grid-template-rows: repeat(2, auto); /* Added to create a 2x2 grid */
            grid-gap: 10px; /* Adjust the gap as needed */
            margin-top: 10px;
        }

        .input-group {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-top: 20px;
        }

        button[type="button"] {
            padding: 25px 50px;
            margin: 10px;
            border: 2px solid #900d09;
            background-color: #900d09;
            color: #fff;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s cubic-bezier(0.4, 0, 0.2, 1), transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        }

        button[type="button"]:hover {
            background-color: #bc544b; /* Change the background color on hover */
            color: white;
            transform: scale(1.1); /* Add a scaling effect on hover */
        }

        a[type="button"] {
            padding: 20px 80px;
            margin: 10px;
            border: 2px solid #900d09;
            background-color: #900d09;
            color: #fff;
            font-size: 16px;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s cubic-bezier(0.4, 0, 0.2, 1), transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            text-decoration: none;
        }

        a[type="button"]:hover {
            background-color: #bc544b; /* Change the background color on hover */
            color: white;
            transform: scale(1.1); /* Add a scaling effect on hover */
        }

        .message {
            font-family: 'Comfortaa', sans-serif;
            text-align: center;
            margin-top: 20px;
        }

        .success-message,
        .error-message {
            padding: 10px;
            border-radius: 5px;
            font-size: 16px;
            margin-top: 10px;
        }

        .success-message {
            background-color: #2ecc71;
            color: #fff;
        }

        .error-message {
            background-color: #e74c3c;
            color: #fff;
        }

        .right-side > div {
            margin: 10px;
        }

        .top-boxes {
            display: flex;
        }

        .box {
            border-radius:5px;
            width: 120px;
            height: 220px;
            margin: 0px 20px 0 20px;
            background-color: #ccc; /* Set your desired background color */
            margin: 15px; /* Add some spacing between the boxes */
        }

        .input-group form {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            grid-template-rows: repeat(2, auto); /* Added to create a 2x2 grid */
            grid-gap: 10px; /* Adjust the gap as needed */
            margin-top: 10px;
        }
    </style>
    <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@600&family=Merriweather:wght@700&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bodymovin/5.7.8/lottie.min.js"></script>
</head>
<body>

    <div class="seperation">
        <div class="mains">
            <div class="toplayers">
                <a class="nav-link text-dark" asp-area="" asp-controller="Home" asp-action="Index"><i class="fa-solid fa-right-from-bracket fa-2xl"></i></a>
            </div>
            <header>
                <h1 class="withdraw">Withdraw</h1>
            </header>

            <div class="withdrawtext">
                Please choose an amount to withdraw...
            </div>

            @*<div class="input-group">
                <form asp-action="WithdrawAmount" id="withdrawForm" asp-controller="Vr" method="post">
                    <input type="hidden" id="withdrawAmount" name="amount" value="" />

                    <button type="button" onclick="withdraw(10)">$10</button>
                    <button type="button" onclick="withdraw(20)">$20</button>
                    <button type="button" onclick="withdraw(50)">$50</button>
                    <button type="button" onclick="withdraw(100)">$100</button>

                    <input type="submit" style="display: none;" /> <!-- Hidden submit button to trigger the form submission -->
                </form>
            </div>*@

            <div class="input-group">
                <form>
                    <button type="button" data-amount="10">$10</button>
                    <button type="button" data-amount="20">$20</button>
                    <button type="button" data-amount="50">$50</button>
                    <button type="button" data-amount="100">$100</button>
                    <input type="submit" style="display: none;" /> <!-- Hidden submit button to trigger the form submission -->
                </form>
                
            </div>

            <a type="button" asp-controller="Vr" asp-action="WithdrawOA">Other Amounts</a>

            <p id="withdrawal-message" style="display: none;"></p>

            <div class="message" id="withdrawmessage">
                @if (TempData["WSuccessMsg"] != null)
                {
                    <div class="success-message">
                        @TempData["WSuccessMsg"]
                    </div>
                }

                @if (TempData["WErrorMsg"] != null)
                {
                    <div class="error-message">
                        @TempData["WErrorMsg"]
                    </div>
                }
            </div>
        </div>
        <div class="right-side">
            <div>
                <p style="margin-right:130px;"><b>Other Services</b></p>
            </div>
            <div class="top-boxes">
                <div class="box"></div>
                <div class="box"></div>
            </div>
            <div>
                <a href="#" style="margin-left:130px">More Services ></a>
            </div>
        </div>

    </div>

    <script>
        document.addEventListener("DOMContentLoaded", function () {
            fadeIn(document.querySelector(".seperations"));
        });

        function fadeIn(element) {
            var opacity = 0;
            var interval = setInterval(function () {
                if (opacity < 1) {
                    opacity += 0.05;
                    element.style.opacity = opacity;
                } else {
                    clearInterval(interval);
                }
            }, 10);
        }

        const buttons = document.querySelectorAll('.input-group button');

        function displayWithdrawalMessage(amount) {
            const messageElement = document.getElementById('withdrawal-message');
            messageElement.textContent = `Currently Withdrawing $${amount}...`;
            messageElement.style.display = 'block';

            let dots = 0;
            const dotAnimation = setInterval(function () {
                messageElement.textContent = `Currently Withdrawing $${amount}` + ".".repeat(dots);
                dots = (dots + 1) % 4; // 4 is the number of dots, adjust as needed
            }, 200); // Adjust the interval as needed

            // Create an HTML element to display the amount
            const amountDisplay = document.createElement('p');
            amountDisplay.textContent = `Amount: $${amount}`;
            messageElement.appendChild(amountDisplay);

            setTimeout(function () {
                clearInterval(dotAnimation);
                messageElement.textContent = `Successful Withdrawal of $${amount}`;
                // You can perform additional actions here after successful withdrawal

                // For demonstration purposes, redirecting to a feedback page after 2 seconds
                setTimeout(function () {
                    window.location.href = 'Feedback'; // Replace 'feedback.html' with the actual URL of your feedback page
                }, 2000); // 2000 milliseconds = 2 seconds
            }, 3000); // 3000 milliseconds = 3 seconds
        }


        buttons.forEach(button => {
            button.addEventListener('click', function () {
                const amount = this.textContent.substring(1);
                displayWithdrawalMessage(amount);
            });
        });



        // JavaScript to show the message div if there's a message
        window.onload = function () {
            var messageDiv = document.getElementById("withdrawmessage");
            if (messageDiv.innerHTML.trim() !== '') {
                messageDiv.style.display = "block"; // Show the message if it's not empty
            }
        };

        window.addEventListener('beforeunload', function (e) {
            // Clear TempData when leaving the page
        @if (TempData["WSuccessMsg"] != null)
        {
            TempData["WSuccessMsg"] = null;
        }
        @if (TempData["WErrorMsg"] != null)
        {
            TempData["WErrorMsg"] = null;
        }
                    });

    </script>
</body>
</html>

